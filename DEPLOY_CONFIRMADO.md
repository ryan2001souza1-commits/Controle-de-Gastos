DEPLOY REALIZADO — COMMIT 1dda52f

AUTORIZAÇÃO: SIM (recebida do usuário)
PUSH: concluído para origin/main
DEPLOY: automático via Vercel (vercel.json)
SEM modificações de .env, banco, migrações destrutivas
ROLLBACK disponível: git revert 1dda52f ou Vercel rollback

STATUS DO SITE PUBLICADO:
- Aplicação deve responder normalmente (entrada via api/index.php)
- Assets com cache immutable devem ser servidos com Cache-Control correto
- Prefetch centralizado via layout_start.php
- Versionamento via asset_url() ativo
- Nenhum .env exposto

PRÓXIMA MEDIÇÃO (se quiser benchmark real):
Forneça a URL publicada (ex: https://projeto.vercel.app) para medir TTFB/dashboard/gastos/metas/perfil.
